# Local development overrides — only active when remote ES is NOT accessible.
# When on VPN / corporate network, comment these lines out so .env is used.

# ES_HOST=http://localhost:9200
# ES_USERNAME=
# ES_PASSWORD=
